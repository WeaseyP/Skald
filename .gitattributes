# Normalize line endings: LF in the repo, native on checkout.
* text=auto

# Windows batch files must stay CRLF (cmd.exe misparses LF-only labels).
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binaries — never normalize.
*.exe binary
*.wasm binary
*.png binary
*.jpg binary
*.ico binary
*.icns binary
